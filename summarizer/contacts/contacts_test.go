package contacts

import "testing"

// TODO build this out
func TestGetAssetIdsHappyPath(t *testing.T) {

	//I want the test to pass in a alertEventId string into GetAssetIds
	//Then I want it to have a mocked HTTP server return a json object
	//The Json object should just be a list of strings like "asset1", "asset2" etc
	//I want an assert to check that the slice is equal to the items in the json
}
